import Betslip from '../../../model/Betslip';

const state = {
  betslip: new Betslip(),
  activeAreaIndex: 0,
  readyBetslipsNumbers: 0, // Default value for ready betslips numbers
};

export default state;
