const actionTypes = {
  SET_LANGUAGE: 'SET_LANGUAGE'
}

const mutationTypes = {
  SET_LANGUAGE: 'SET_LANGUAGE'
}

const getterTypes = {
  GET_LANGUAGE: 'GET_LANGUAGE'
}

const types = {
  mutations: { ...mutationTypes },
  actions: { ...actionTypes },
  getters: { ...getterTypes }
}

export default types
